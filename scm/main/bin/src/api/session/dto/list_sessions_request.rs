/// Input for [`crate::api::session::SessionRepository::list`]. Carries no
/// fields today — named (rather than passing no argument at all) so the
/// contract can grow filters later without a breaking signature change.
pub struct ListSessionsRequest;
